  <div class="search-result">

  <div class="search-result-title">問合せ一覧</div>

  <div class="search-result-index container" style ="color:gray;">


   <% @inquiries.each do |i| %>
      <div class ="row">
        <div class = "col-12"><i class="fas fa-user-circle"></i><%= Member.find_by(id: i.user_id).name%> &nbsp;
         <i class="far fa-envelope"></i><%= Member.find_by(id: i.user_id).email%></div>
          <div class = "col-12" style="text-align:center;"><i class="far fa-paper-plane"></i>「<%= i.content%>」</p>
          <div class = "col-12" style="text-align:right;"><i class="far fa-clock"></i> <%=i.created_at.strftime("%c")%></div>
    </div>
    <% end %>

  </div>
</div>